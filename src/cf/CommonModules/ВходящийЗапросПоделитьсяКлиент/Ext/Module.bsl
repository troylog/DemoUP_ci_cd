Процедура ОбработкаВыполненияСвоейКоманды(ДанныеЗапроса) Экспорт
	
	Если ДанныеЗапроса.Файлы.Количество() > 0 Тогда
		Для каждого Файл из ДанныеЗапроса.Файлы Цикл

			ПараметрыФормы = Новый Структура;
			ПараметрыФормы.Вставить("ПутьКЗагружаемомуФайлу", Файл);
			ФормаЭлемента = ПолучитьФорму("Справочник.ХранимыеФайлы.Форма.ФормаЭлемента", ПараметрыФормы);
			ФормаЭлемента.Открыть();
			///33
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры